# Choice UI prefers Discord Components

Bounded choices use Buttons, Select Menus, and Modals. Chat Input remains the fallback for open-ended text in Pane Threads. Rejected parsing yes/no from free text when Components can express the choice.

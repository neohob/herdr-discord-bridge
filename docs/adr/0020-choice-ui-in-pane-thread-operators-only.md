# Choice UI posts in the Pane Thread; Operators only

When Herdr needs a bounded decision (e.g. blocked), the Bot posts Buttons/Select/Modal in that Pane Thread. Only Operators may interact; others are rejected. Chat Input remains the fallback. Rejected guild-wide click access and posting decisions only on the Remote Channel (wrong context).

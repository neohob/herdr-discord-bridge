# Chat Input sends all user messages in Pane Threads

In a Pane Thread, every message from a human user is forwarded to the Herdr Pane (`pane.send_input` with Enter). The Bot ignores its own messages. Structured control stays on `/herdr`. Rejected prefix-gated send as the default for a private operator Thread (extra friction).

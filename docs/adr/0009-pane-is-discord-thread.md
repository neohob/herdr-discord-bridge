# Pane is a Discord Thread under the Remote Channel

A Remote is one guild text channel; each Pane is a Thread under that channel. Matches “channel = host, sub-channel = pane.” Rejected Category + child text channels (weaker “inside the channel” model). Threads carry Terminal Messages and Chat Input.

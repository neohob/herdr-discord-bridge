# Explicit sync for existing Panes

Connecting to a Gateway does not automatically open a Thread per existing Herdr Pane. Operators run `/herdr sync` (or an equivalent explicit step) to materialize Threads and enable observe. Rejected auto-map-all-on-connect (Thread spam) and ignore-existing-panes (blind to the live herd).

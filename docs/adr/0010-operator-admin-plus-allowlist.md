# Operators: admin gate plus optional allowlist

Remote registration and structural `/herdr` ops require Discord admin-style permission (Manage Guild or a configured role), plus an optional user-id allowlist in Bot config. Fits a private guild today and future co-developers without opening token-setting to every member. Slash replies for secrets stay ephemeral so tokens are not left in channel history.

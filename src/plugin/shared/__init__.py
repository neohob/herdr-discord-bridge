"""Shared helpers vendored for standalone Herdr plugin installs.

When the plugin is installed via ``herdr plugin install owner/repo/src/plugin``,
only this directory is present. Keep these modules in sync with ``src/shared/``.
"""
